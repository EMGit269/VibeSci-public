// This component is deprecated and has been replaced by ProjectBoard.
// It is kept here to avoid breaking changes if it was imported elsewhere,
// but it no longer contains any functional code.
export function TaskBoard() {
    return null;
}
